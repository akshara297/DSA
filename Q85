def climb_stairs(n):
    if n <= 2: return n
    one_step_behind, two_steps_behind = 2, 1
    for _ in range(3, n + 1):
        curr = one_step_behind + two_steps_behind
        two_steps_behind = one_step_behind
        one_step_behind = curr
    return one_step_behind
